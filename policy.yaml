users:
  # EXAMPLE USERS - EDIT THESE LINES!
  # Change 'sonchan' to your actual OS username
  sonchan: admin        # ← CHANGE 'sonchan' TO YOUR USERNAME
  bob: operator       # ← CHANGE 'bob' IF NEEDED
  charlie: auditor    # ← CHANGE 'charlie' IF NEEDED
  
  # System users (keep these)
  root: admin
  admin: admin

roles:
  admin:
    - init
    - backup
    - list-snapshots
    - verify
    - restore
    - audit-verify
  
  operator:
    - backup
    - list-snapshots
    - verify
    - restore
    - audit-verify
  
  auditor:
    - list-snapshots
    - verify
    - audit-verify